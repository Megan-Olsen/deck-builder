const cards = require('../data.json')

const deck = {total: 0, items: []}
let deckId = 0

module.exports = {
    getDeck : (req, res) => {},
    addToDeck : (req, res) => {},
    changeQuantity : (req, res) => {},
    removeFromDeck : (req, res) => {},
    delete : (req, res) => {}



}
